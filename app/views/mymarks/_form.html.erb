<%= form_for(@mymark) do |f| %>
  <% if @mymark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mymark.errors.count, "error") %> prohibited this mymark from being saved:</h2>

      <ul>
      <% @mymark.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>

<div class="field">
<%= f.label :mark_id %>
<%= f.select :mark_id, @marks.map{|mark| [mark.name, mark.id]} %>
</div>  

<div class="actions">
    <%= f.submit %>
  </div>
<% end %>
